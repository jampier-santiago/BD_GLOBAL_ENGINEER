// --- Modelos ---
const personasModel = require("./personas");
const produccionesModel = require("./producciones");
const informesModel = require("./informes");
const productosModel = require("./productos");

const modelos = {
  personasModel,
  produccionesModel,
  informesModel,
  productosModel,
}

module.exports = modelos;